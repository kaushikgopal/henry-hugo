{{- $pctx := . -}}
{{- if .IsHome -}}
    {{ $pctx = .Site }}
{{- end -}}

{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
    {{- $pages = $pctx.RegularPages -}}
{{- else -}}
    {{- $pages = $pctx.Pages -}}
{{- end -}}

{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
    {{- $pages = $pages | first $limit -}}
{{- end -}}

{{- $title := .Site.Title -}}
{{- if ne .Title  .Site.Title -}}
    {{- if or (eq .Title  "Blogs") (eq .Title "Bs") -}}
        {{- $title = (print .Site.Title " - Blog posts") -}}
    {{- else -}}
        {{- $title = (print .Site.Title " - " .Title)  -}}
    {{- end -}}
{{- end -}}

{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
    <channel>

        <title>{{ $title }}</title>
        <link>{{ .Permalink }}</link>
        <description>Recent content - {{ $title }}</description>
        <generator>Hugo -- gohugo.io</generator>
        {{ with .Site.LanguageCode }}<language>{{.}}</language>{{end}}
        {{ with .Site.Author.email }}<managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}
        {{ with .Site.Author.email }}<webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}
        {{ with .Site.Copyright }}<copyright>{{.}}</copyright>{{end}}
        {{ if not .Date.IsZero }}<lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}

        {{ if (fileExists "static/images/avatar.png") }}
            <image>
                <url>{{.Site.BaseURL}}images/avatar.png</url>
                <title>{{ $title }}</title>
                <link>{{ .Permalink }}</link>
            </image>
        {{ else if (fileExists "static/images/favicon.png") }}
            <image>
                <url>{{.Site.BaseURL}}images/avatar.png</url>
                <title>{{ $title }}</title>
                <link>{{ .Permalink }}</link>
            </image>
        {{ end }}

        {{- with .OutputFormats.Get "RSS" -}}
        {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
        {{- end -}}

        {{ range $pages }}
            <item>
                <title>{{ .Title }}</title>
                <link>{{ .Permalink }}</link>
                <pubDate>{{ .Date.Format "Mon, 02 Jan 2006" | safeHTML }}</pubDate>
                {{ with $.Page.Lastmod }}<lastBuildDate>{{ .Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{end}}
                {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
                <guid>{{ .Permalink }}</guid>

                <description>
                    {{ with .Params.banner }}
                        {{ printf "<![CDATA[<img src=\"%s%s\" />]]>" $.Site.BaseURL . | safeHTML }}
                    {{ end }}
                    {{ .Content | html }}
                </description>
            </item>
        {{ end }}
    </channel>
</rss>